<Window x:Class="KioskSkytel.KioskApp.UI.Views.Cally.Payment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KioskSkytel.KioskApp.UI.Views.Cally"
        mc:Ignorable="d"
        Title="Payment"
        Height="620"
        Width="440"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        KeyDown="Window_KeyDown">

    <Window.Resources>

        <!-- Number button -->
        <Style x:Key="NumPadButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#1A1A2E"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                BorderBrush="#E8EDF3"
                                BorderThickness="0.5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F1F5FB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E3ECF8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Delete button -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="#DC2626"/>
            <Setter Property="Background" Value="#FEF2F2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                BorderBrush="#FECACA"
                                BorderThickness="0.5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FEE2E2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FECACA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- OK button -->
        <Style x:Key="OkButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1565C0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close button -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Content">
                <Setter.Value>
                    <TextBlock Text="✕" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="16"
                                BorderBrush="#E5E7EB"
                                BorderThickness="0.5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Cursor blink animation -->
        <Style x:Key="CursorStyle" TargetType="Border">
            <Style.Triggers>
                <EventTrigger RoutedEvent="Border.Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever">
                            <DoubleAnimation
                                Storyboard.TargetProperty="Opacity"
                                From="1" To="0" Duration="0:0:0.6"
                                AutoReverse="True"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <!-- Outer shadow + rounded window -->
    <Border Background="White"
            CornerRadius="24"
            BorderBrush="#DDE3ED"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.10" BlurRadius="32" ShadowDepth="4" Direction="270"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    BorderBrush="#EEF0F4"
                    BorderThickness="0,0,0,1"
                    Padding="20,0">
                <Grid>
                    <TextBlock Text="Гэрээний дугаар оруулна уу"
                               FontSize="15"
                               FontFamily="Segoe UI"
                               FontWeight="Medium"
                               Foreground="#1A1A2E"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                    <Button Style="{StaticResource CloseButtonStyle}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <!-- Body -->
            <StackPanel Grid.Row="1"
                        Margin="28,24,28,32"
                        VerticalAlignment="Top">

                <!-- Display field -->
                <Border Background="#F8FAFC"
                        CornerRadius="12"
                        BorderBrush="#DDE3ED"
                        BorderThickness="0.5"
                        Height="60"
                        Padding="18,0"
                        Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="TxtDisplay"
                                   Grid.Column="0"
                                   FontSize="26"
                                   FontWeight="Medium"
                                   Foreground="#1A1A2E"
                                   VerticalAlignment="Center"
                                  />
                        <!-- Blinking cursor -->
                        <Border Grid.Column="1"
                                Width="2"
                                Height="26"
                                Background="#1565C0"
                                CornerRadius="1"
                                VerticalAlignment="Center"
                                Style="{StaticResource CursorStyle}"/>
                    </Grid>
                </Border>

                <!-- Hint -->
                <TextBlock Text="Дугаараа оруулаад OK дарна уу"
                           FontSize="12"
                           Foreground="#9CA3AF"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- Numpad -->
                <UniformGrid x:Name="NumPadGrid" Columns="3" Rows="4">

                    <Button Content="1" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="0,0,5,5" Click="NumBtn_Click" Tag="1"/>
                    <Button Content="2" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="2.5,0,2.5,5" Click="NumBtn_Click" Tag="2"/>
                    <Button Content="3" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="5,0,0,5" Click="NumBtn_Click" Tag="3"/>

                    <Button Content="4" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="0,0,5,5" Click="NumBtn_Click" Tag="4"/>
                    <Button Content="5" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="2.5,0,2.5,5" Click="NumBtn_Click" Tag="5"/>
                    <Button Content="6" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="5,0,0,5" Click="NumBtn_Click" Tag="6"/>

                    <Button Content="7" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="0,0,5,5" Click="NumBtn_Click" Tag="7"/>
                    <Button Content="8" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="2.5,0,2.5,5" Click="NumBtn_Click" Tag="8"/>
                    <Button Content="9" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="5,0,0,5" Click="NumBtn_Click" Tag="9"/>

                    <Button Content="0" Height="64" Style="{StaticResource NumPadButtonStyle}" Margin="0,0,5,0" Click="NumBtn_Click" Tag="0"/>
                    <Button Height="64" Style="{StaticResource DeleteButtonStyle}" Margin="2.5,0,2.5,0" Click="ClearBtn_Click">
                        <TextBlock Text="⌫" FontSize="20" VerticalAlignment="Center"/>
                    </Button>
                    <Button Content="OK" Height="64" Style="{StaticResource OkButtonStyle}" Margin="5,0,0,0" Click="OkBtn_Click"/>

                </UniformGrid>

            </StackPanel>
        </Grid>
    </Border>
</Window>
